<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>DialogDelayMeasureHelp</class>
 <widget class="QDialog" name="DialogDelayMeasureHelp">
  <property name="geometry">
   <rect>
    <x>0</x>
    <y>0</y>
    <width>458</width>
    <height>205</height>
   </rect>
  </property>
  <property name="windowTitle">
   <string>How to perfomr stream delay calculations?</string>
  </property>
  <layout class="QGridLayout" name="gridLayout">
   <item row="0" column="0">
    <widget class="QLabel" name="label">
     <property name="text">
      <string>1. Make your camera to watch your monitor.
2. Select a point on the playback area where you monitor is visible.
3. Choose test red.
	The full screen will go red, 
	and after when this screen 
	disappear a message will be shown. 
4. If the the testing succeded, test the blue too. 
	If not make sure that your screen 
	is visible under the calibration point.
5. If both colors were passed. Click on the delay measurement.
</string>
     </property>
     <property name="alignment">
      <set>Qt::AlignLeading|Qt::AlignLeft|Qt::AlignTop</set>
     </property>
    </widget>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections/>
</ui>
